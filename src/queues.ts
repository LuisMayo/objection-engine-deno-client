export enum Queue {
  /**
   * Queue for fast operations like getting the available music.
   */
  fastQueue = "oe_fast",
  /**
   * Queue for slow operations like rendering
   */
  slowQueue = "oe_slow",
}
